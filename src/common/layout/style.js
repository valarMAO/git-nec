import styled from 'styled-components';

export const Logo = styled.div`
	float: left;
	color: #fff;
	cursor: pointer;
`;

export const Avatars = styled.div`
	float: right;
	color: #fff;
	cursor: pointer;
`;
